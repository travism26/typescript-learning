const today = new Date();
today.getMonth();
// shortcut to say here are the different properties and methods 
// that this values has!

const person =  {
  age: 20
};

person.age

class Color {
  
}

// typescript knows that this belongs to the 
// class red (nothing in there but its linked)
const red = new Color();

// Where will we use this in a normal project?
/**
 * Use it everywhere, this is a side affect of using typescript
 * types are used everywhere and by default we use it everywhere
 */
